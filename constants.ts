/**
 * This variable is used to know if content is served through server or client
 */
export const IS_CLIENT = typeof window !== 'undefined';

/**
 * Get the app bar height.
 */
export const APP_BAR_HEIGHT = 100;
